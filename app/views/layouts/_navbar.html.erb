<nav class="navbar navbar-default">
	<div class="container-fluid">
		<div class="navbar-header">
			<div class="navbar-brand"> <%= link_to "ToolChest", tools_path %> </div>
		</div>
		<ul class="nav navbar-nav navbar-right">
		<li class="active">
		<a href="/cart"><span class="glyphicon glyphicon-shopping-cart glyphy-butt"></span><%= pluralize(@cart.quantity, "Item") %> - <%= number_to_currency(@cart.total, :precision => 2) %></a>
		</li>
		<li class="active">
				<%= link_to "My Dashboard", dashboard_path(current_user) if current_user %>
		</li>
		<li class="active">
				<%= link_to "Logout", logout_path, method: "delete" if current_user %>
				<%= link_to "Login", login_path if !current_user %>
		</li>
		</ul>
	</div>
</nav>
