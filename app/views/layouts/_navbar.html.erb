<nav class="navbar navbar-dark bg-inverse">
	<div class="logo"> <%= link_to "tiny stay", root_path %> </div>
	  <ul class="nav navbar-nav pull-xs-right">
			<li class="nav-item nav-search">
				<form class="form-inline">
					<%= form_tag homes_path, method: :get, class: "form-group" do %>
			      <%= text_field_tag :location, params[:location], class: "form-control", placeholder: "Where to?" %>
			      <%= submit_tag "Search", name: nil, class: "btn btn-one" %>
			    <% end %>
				</form>
			</li>
	    <li class="nav-item">
	  		<%= link_to "My Dashboard", dashboard_path(current_user.slug), class: "nav-link" if current_user %>
	    </li>
	    <li class="nav-item">
				<%= link_to "Logout", logout_path, method: :delete, class: "nav-link" if current_user %>
				<%= link_to "Login", login_path, class: "nav-link" if !current_user %>
	    </li>
	    <li class="nav-item">
				<a href="/cart" class="nav-link"><span class="glyphicon glyphicon-shopping-cart glyphy-butt"></span><%= pluralize(@cart.quantity, "Reservation") %> - <%= number_to_currency(@cart.total, :precision => 2) %></a>
			</li>
	  </ul>
</nav>
